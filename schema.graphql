type User @entity {
  id: ID!
  pools: [Pool!]!
}

type Pool @entity {
  id: ID!
  owner: String!
  symbol: String!
  totalDeposit: BigInt!
  history: [BigInt!]!
  users: [User!]!
}

type UserPool @entity {
  id: ID!
  pool: Pool!
  user: User!
  totalDeposit: BigInt!
}
