type User @entity {
  id: ID!
  pools: [Pool!]!
}

type Pool @entity {
  id: ID!

  owner: String!
  symbol: String!
  totalDeposit: BigInt!
  users: [User!]!

  history: [BigInt!]!
  timestamps: [BigInt!]!
  privatePool: Boolean!
}

type UserPool @entity {
  id: ID!
  pool: Pool!
  user: User!
  totalDeposit: BigInt!

  history: [BigInt!]!
  timestamps: [BigInt!]!
}
